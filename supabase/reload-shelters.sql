-- 대피소를 새로 올리기 전에 기존 것을 비운다.
--
-- shelters_upload.csv 에 민방위와 지진이 모두 들어 있으므로,
-- 그냥 올리면 민방위가 중복된다. 먼저 비우고 통째로 올린다.
--
-- 이동 기록(trips)과 인원 집계는 대피소를 참조하고 있어 함께 사라진다.
-- 지금 이동 중인 사람이 있으면 그 기록만 없어질 뿐,
-- 다시 길찾기를 누르면 정상 동작한다.

delete from shelters;

-- 확인: 0 이어야 한다
select count(*) as 남은건수 from shelters;
